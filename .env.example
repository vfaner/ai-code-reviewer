# docker compose 环境变量示例
# 复制为 .env 后修改；生产环境务必替换两个密钥

# JWT 签名密钥（至少 32 字符的随机字符串）
APP_JWT_SECRET=please-change-this-to-a-random-secret-at-least-32-chars

# 敏感配置加密密钥（AES，必须为 16 个字符）
APP_CRYPTO_KEY=a1b2c3d4e5f6g7h8

# 关闭登录鉴权（仅限内网演示）：false
APP_AUTH_ENABLED=true
